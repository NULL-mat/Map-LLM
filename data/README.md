# Demo data

The public repository does not distribute source GIS datasets or their metadata.
Use your own Shapefile, GeoJSON, or GeoPackage files and set `DATA_DIRECTORY_BASE`
to a local directory.  The private project keeps the original datasets used by
the internal demonstrations.
