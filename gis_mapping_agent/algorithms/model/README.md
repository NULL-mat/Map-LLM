# Optional model assets

Trained model weights are intentionally excluded from this public repository.
The private deployment may place a compatible model behind the
`GCNNSelector` interface without changing the public Agent, state, or tool
contracts.
